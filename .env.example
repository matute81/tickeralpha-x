# Financial Modeling Prep. Never commit the real key.
# https://site.financialmodelingprep.com/developer/docs
FMP_API_KEY=

# Required for X ideas — jobs will not run without one of these (Anthropic preferred if both are set).
# GitHub Actions uses the same names as repository secrets. CURSOR_API_KEY is not used.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
